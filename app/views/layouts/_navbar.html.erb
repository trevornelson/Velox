<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
        <span class="sr-only">Toggle navigation<span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/"><i class="fa fa-paper-plane-o"></i></a>
    </div>

    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if @session_user %>
          <li><%= link_to 'my account', curr_user_path %></li>
          <li><%= link_to 'logout', curr_session_path, method: :delete %></li>
        <% else %>
          <li><a href="#" data-toggle="modal" data-target="#login-modal">log in</a></li>
          <li><a href="#" data-toggle="modal" data-target="#signup-modal">sign up</a></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
