/**
Service for user authentication and querying app backend for user data.
**/
App.service('UserSvc', ['$rootScope',
                function($rootScope) {

  var service = {


  };

  return service;
}]);
